CUPS_HOST=localhost
CUPS_PORT=631
PORT=8034
# Path to printer whitelist file, one pattern per line, * wildcards supported (e.g. GetSteps*).
# Defaults to ./printer-whitelist.txt. Missing file or empty = no restriction. Re-read on every request.
PRINTER_WHITELIST_FILE=
